# Level 10 - chmod

## Objective
Cambiar permisos para ejecutar un script que imprime la flag.

## Skills learned
- `chmod` numerico
- `chmod` simbolico

## Description
El archivo existe pero no tiene permiso de ejecucion inicial.

## Hidden flag
Generada por `setup.sh` y usada como password de `dw11`.

## Optional hints
- Verifica permisos con `ls -l`.
